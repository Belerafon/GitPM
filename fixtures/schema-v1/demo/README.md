# Deterministic GitPM schema v1 demo portfolio

This repository fixture intentionally uses fixed IDs and dates.

`uploads/` is an allowed non-domain input directory. Git tracks only its
`.gitkeep`; incoming files are intentionally ignored.
