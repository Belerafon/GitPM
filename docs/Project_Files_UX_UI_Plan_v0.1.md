# UX/UI-план файлов проекта v0.1

Статус: предложение на согласование с владельцем продукта. Документ описывает только
пользовательский опыт; контракт репозитория и пошаговый технический план будут подготовлены после
одобрения.

## 1. Назначение и границы

У каждого Project появляется простое пользовательское файлохранилище для договоров, технических
заданий, таблиц, презентаций, сканов, изображений и других рабочих материалов. Оно не заменяет
существующий технический файловый менеджер рабочей копии:

- пользовательское хранилище показывает только файлы текущего Project;
- технический менеджер остаётся низкоуровневым инструментом для произвольной работы с файловой
  системой;
- оригиналы файлов входят в Git-репозиторий и участвуют в обычном процессе черновика, просмотра
  изменений, коммита и публикации;
- расширение файла не используется как список разрешённых типов;
- файл можно хранить, даже если GitPM не умеет показать его содержимое в браузере.

В первой версии хранилище плоское, без создания папок через пользовательский интерфейс. Это
сохраняет однозначность имён и ссылок. Файлы, помещённые техническим менеджером непосредственно в
канонический каталог файлов Project, появляются в пользовательском хранилище после обновления.
Вложенные каталоги до появления отдельного UX остаются зоной технического менеджера.

## 2. Точка входа на странице проекта

Рядом с описанием Project или сразу под ним, перед метаданными проекта, размещается вторичная
кнопка `Файлы`. Она должна восприниматься как часть информационного блока проекта, а не смешиваться
с кнопками редактирования, создания этапа и задачи.

Кнопка содержит:

- пиктограмму скрепки или файловой библиотеки;
- надпись `Файлы`;
- компактный счётчик, включая `0` для пустого хранилища.

Кнопка доступна также в архивном Project и для пользователей только с правом чтения: просмотр и
скачивание остаются полезными. Операции изменения внутри хранилища подчиняются текущему состоянию
черновика, режима записи (writer mode) и роли пользователя.

```text
Project P-26-ABC123
Редизайн личного кабинета заказчика
Заменить устаревший кабинет партнёра.

[ скрепка  Файлы  12 ]

Статус: Активен   Владелец: Анна   Начало: 01.09.2026   Срок: 15.12.2026
```

После успешной загрузки или удаления счётчик обновляется сразу, а при следующем обновлении рабочей
области сверяется с фактическим содержимым репозитория.

## 3. Правая панель файлов

Нажатие `Файлы` открывает отдельную панель справа поверх текущего контекста Project. Перехода на
другую страницу не происходит, выбранная Task не сбрасывается. После закрытия пользователь
возвращается точно в прежний контекст.

Просмотр самого содержимого — в отдельном окне браузера. В этой панели находится только список
файлов.

Состояние библиотеки:

```text
+----------------------------------------------------------------+
| Файлы проекта                                      12       ×   |
| [Поиск по имени...]          Сортировка: Имя    [Загрузить]    |
|                                                                |
|  [PDF]       [DOCX]      [XLSX]      [IMG]      [ФАЙЛ]        |
|  Договор     ТЗ_v3       Смета       Схема      Архив          |
|  2026.pdf    .docx       .xlsx       .png       .zip           |
|                                                                |
| Перетащите сюда файлы для загрузки                             |
+----------------------------------------------------------------+
```

По умолчанию используется похожая на Проводник Windows сетка плиток: крупная пиктограмма, под ней
имя файла. Вид можно переключать с сетки на табличный. Выбор пользователя сохраняется в cookie и
применяется к файловым хранилищам всех Project. Длинное имя переносится максимум на две строки;
полное имя доступно в подсказке и как доступное имя элемента.

Достаточно узнаваемых семейств пиктограмм:

- PDF;
- Word и совместимые текстовые документы;
- Excel и табличные форматы, включая CSV;
- PowerPoint и презентации;
- изображения с безопасной миниатюрой, если она доступна;
- текст, Markdown и исходный код;
- архивы;
- нейтральная пиктограмма для остальных файлов.

В панели инструментов находятся кнопки `Загрузить файлы`, `Переименовать`, `Удалить`, `Свойства`
и `Скачать`. Перетаскивание файлов в панель выполняет ту же операцию загрузки. После открытия файла
и возврата сохраняются запрос поиска, сортировка и позиция прокрутки.

## 4. Просмотр файла

Одинарный клик по плитке открывает файл в новой вкладке, если его безопасно отдать для встроенного
просмотра. Браузер сам показывает поддерживаемое содержимое. Для остальных форматов GitPM сразу
предлагает скачивание.

## 5. Операции с файлами

### Загрузка

Кнопка `Загрузить файлы` поддерживает множественный выбор. Панель показывает очередь, прогресс и
результат отдельно для каждого файла. По окончании загрузки панель не закрывается.

Если имя уже существует, GitPM ничего не перезаписывает молча. Диалог предлагает:

- `Заменить текущий файл` — сохранить прежнее имя и ссылки, создав новую версию в Git;
- `Загрузить под другим именем` — отредактировать предложенное имя;
- `Отмена`.

Разрешены Unicode и пробелы. Следует поддержать обычные допустимые имена Windows, чтобы
пользователю не приходилось переименовывать файлы перед загрузкой. Разделители пути,
зарезервированные имена и другие недопустимые для файловой системы варианты отклоняются с
конкретным пояснением рядом с полем. Внутри одного Project имена сравниваются без учёта регистра и
должны быть уникальными, чтобы репозиторий одинаково работал в Windows и в системах с
чувствительной к регистру файловой системой.

### Файлы больше 50 МБ

Порог 50 МБ является границей предупреждения, а не запретом. Для файла больше порога до начала
передачи открывается отдельное подтверждение:

1. Показать точное имя и отформатированный размер.
2. Объяснить, что оригинал и последующие версии остаются в истории Git, увеличивая время
   клонирования, получения изменений и занимаемое репозиторием место.
3. Потребовать ввести точное имя файла с учётом регистра, чтобы подтвердить намерение загрузки.
4. Активировать кнопку `Всё равно загрузить большой файл` только при полном совпадении имени.

Несколько больших файлов подтверждаются отдельно, чтобы одним введённым именем нельзя было
разрешить посторонний пакет. Во время передачи видны прогресс и доступная отмена. Ограничения
Git-сервера или транспорта могут привести к отдельной понятной ошибке, но GitPM не должен выдавать
предупреждение на 50 МБ за жёсткий лимит.

Отдельная операция позволяет заменить файл новым с другим именем и одновременно обновить ссылки.
Например, при замене `ТЗ_v1.docx` на `ТЗ_v2.docx` все ссылки в Task должны получить новое имя.

### Переименование

Открывается небольшой отдельный диалог. Текущее имя выделено, расширение остаётся видимым.
Изменение расширения вызывает неблокирующее предупреждение о возможном изменении предпросмотра.
Конфликт имени и недопустимое имя обнаруживаются до изменения репозитория.

Ссылки используют имя файла. Поэтому штатное переименование должно в одной операции изменить имя
и обновить все ссылки текущего Project. При ошибке вся операция откатывается.

### Удаление

Для файла без ссылок используется обычное подтверждение с его именем. Если ссылки существуют,
диалог показывает их количество и короткий список Task и комментариев, где используется файл.
Явное действие называется `Удалить файл и отвязать N ссылок`; при отвязке видимое имя сохраняется
в тексте как обычный текст, окружающая фраза не удаляется.

Удаляется файл из текущей версии репозитория. Диалог объясняет, что ранее закоммиченное содержимое
может сохраниться в истории Git. Интерфейс не должен обещать безопасное физическое уничтожение.

### Свойства

Свойства показываются компактной панелью с копируемыми значениями:

- имя и определённый формат;
- человекочитаемый размер и точное число байтов;
- канонический путь относительно репозитория, например
  `projects/P-26-ABC123/files/ТЗ_v3.docx`;
- даты создания, изменения;
- количество ссылок внутри Project;
- доступность предпросмотра.

Название даты должно соответствовать её реальному источнику. Время создания файла в конкретной
рабочей копии нельзя выдавать за устойчивую дату создания. Если это возможно без отдельного
реестра, файлу выставляется дата изменения, которую передал браузер из файловой системы
пользователя.

## 6. Ссылки на файлы в тексте Project

Символ `@` остаётся за людьми. Для файлов используется wiki-синтаксис, не зависящий от языка
интерфейса:

```text
Согласно пункту 5.2 [[file:ТЗ_v3.docx]], срок приёмки составляет 10 дней.
```

После ввода `[[` в Markdown-редакторе с контекстом Project появляется список файлов с
пиктограммой, именем и размером. Кнопка со скрепкой рядом с редактором открывает тот же выбор для
пользователей, которые не знают синтаксис. Выбранная ссылка вставляется в позицию курсора. Поиск
не учитывает регистр и находит любую часть имени.

Ссылка хранится по имени из примера выше. Одинаковые имена в пользовательском хранилище запрещены,
поэтому разрешение ссылки однозначно. Штатная операция переименования или замены новым файлом
обновляет такие ссылки атомарно.

В отрисованном тексте ссылка выглядит как компактная ссылка-файл с пиктограммой типа и именем.
Клик открывает файл в новой вкладке или начинает скачивание по тем же правилам, что клик в панели.
Отсутствующая ссылка отображается явным предупреждающим элементом с объяснением, а не неработающей
обычной ссылкой и не молча изменённым текстом.

Поддержка распространяется на Markdown-поля с однозначным контекстом Project, первоначально:

- описание Project;
- описание Milestone;
- описание и критерии приёмки Task;
- комментарии Task;
- заметки о трудозатратах, привязанные к Project и Task.

Глобальные сущности и текст без контекста Project не могут использовать неявные ссылки на файлы
проекта. Межпроектные ссылки не входят в первую версию и не должны молча расширять область Project.

## 7. Черновик, Git и внешние изменения

Загрузка, замена, переименование и удаление являются обычными мутациями черновика и подчиняются
оптимистичной версии (fingerprint) и режиму записи (writer mode).

Экран изменений группирует файлы под их Project и использует понятные подписи: `Файл добавлен`,
`Файл заменён`, `Файл переименован`, `Файл удалён` — для бинарных файлов. Для текстовых показывается
Git diff.

Пользовательское хранилище читает реальный канонический каталог Project. Обычный файл, добавленный,
переименованный или заменённый через технический менеджер, появляется после обновления. Если
внешняя операция создаёт неоднозначное имя, неподдерживаемую вложенность или несогласованные
метаданные, интерфейс показывает исправимое предупреждение, а не угадывает и не скрывает состояние
репозитория.


Отдельной базы данных или реестра для списка файлов, дат создания или авторов в GitPM быть не
должно. Если для возможности потребуются такие скрытые метаданные, от возможности следует
отказаться. Источник истины — файлы в папке Project «как есть».

## 8. Пошаговый технический план реализации

План намеренно разбит на небольшие законченные этапы. Каждый этап выполняется в отдельном рабочем
контексте, заканчивается тематическими тестами, проверкой diff и отдельным коммитом. Следующий этап
начинается только с чистого рабочего дерева. Если по фактическим зависимостям этап окажется слишком
большим, его следует разделить по границе server/web, не объединяя соседние этапы.

### Базовые технические решения

- Каноническое хранилище — обычные файлы в `projects/<project-id>/files/`. Никаких manifest,
  sidecar-файлов, отдельных YAML-сущностей или базы данных со списком файлов не создаётся.
- Имя файла является его идентификатором. Это отменяет ранее предложенный скрытый постоянный ID:
  без отдельного реестра связать такой ID с текущим именем невозможно. Штатное переименование или
  замена с новым именем должны атомарно переписывать ссылки `[[file:...]]` во всём Project.
  Внешнее переименование через технический менеджер может оставить явно показанную сломанную
  ссылку.
- Git не хранит время создания и изменения файла. Поэтому GitPM не сохраняет эти даты отдельно:
  показывает доступные даты текущей рабочей копии и вычисляемые даты Git с точной подписью
  источника. При загрузке можно передать `lastModified` браузера и выставить mtime рабочей копии,
  но нельзя обещать его сохранение после нового checkout.
- Хранить можно любой обычный файл. Для открытия сервер отдаёт безопасный MIME и
  `Content-Disposition: inline` только форматам, которые допустимо показывать в браузере; для
  остальных используется скачивание. Таким образом браузер сам показывает поддерживаемые PDF,
  изображения и текст, но GitPM не исполняет активное содержимое.
- 50 МБ — только порог осознанного подтверждения. Передача должна быть потоковой, без base64.
  Возможный эксплуатационный лимит сервера настраивается отдельно и возвращает понятную ошибку,
  но не маскируется под ограничение в 50 МБ.

### Этапы

| № | Законченный кусок | Проверяемый результат |
| --- | --- | --- |
| 1 | Закрепить формат каталога и правила валидации | Репозиторий принимает плоский каталог `files` внутри Project, обычные файлы любых расширений и Windows-совместимые имена; считает их непрозрачным содержимым, а не domain YAML; отклоняет symlink, вложенные каталоги, обход пути и конфликт имён без учёта регистра. Обновлены нормативные документы и тесты repository/validation. |
| 2 | Сделать read-only слой файлов Project | Domain/server умеют посчитать файлы, вернуть список, размер и доступные свойства, а также безопасно открыть или скачать один файл. Все пути жёстко ограничены текущим Project. Покрыты MIME, Unicode, пустой каталог, отсутствующий файл и hostile paths. |
| 3 | Сделать потоковую загрузку на сервере | Загрузка и замена содержимого работают через UI writer mode и fingerprint, не используют base64, не перезаписывают файл молча и откатываются при ошибке полной проверки репозитория. Сервер проверяет подтверждение файлов больше 50 МБ и возвращает стабильные коды конфликтов. |
| 4 | Сделать переименование и удаление на сервере | Обе операции соблюдают Project scope, fingerprint, защиту от symlink и конфликтов регистра. Пока ссылок ещё нет, API уже имеет явные режимы для будущей проверки и отвязывания ссылок. |
| 5 | Добавить кнопку и базовую правую панель | На странице Project видны `Файлы` и счётчик. Панель показывает пустое, загрузочное и ошибочное состояния, плитки с иконками и табличный вид. Выбор вида хранится в cookie пользователя и применяется ко всем Project. Этот этап пока только читает файлы. |
| 6 | Подключить пользовательскую загрузку | Работают выбор нескольких файлов, drag-and-drop, очередь и прогресс. Конфликт имени предлагает заменить или выбрать другое имя. Для каждого файла больше 50 МБ требуется точный ввод имени. |
| 7 | Подключить обычные действия с файлом | Выбор файла открывает новую вкладку либо скачивание согласно ответу сервера. Из панели работают скачать, свойства, переименовать и удалить. Свойства показывают только реально доступные файловые и Git-данные с указанием источника. |
| 8 | Ввести общий синтаксис файловых ссылок | Общий parser и domain-поиск распознают `[[file:имя]]`, находят все использования только в текущем Project и явно различают существующие и сломанные ссылки. Покрыты Unicode, экранирование, вредоносный ввод и запрет межпроектного расширения области. |
| 9 | Подключить ссылки к основным описаниям | Общий безопасный renderer, подсказки после `[[` и кнопка-скрепка работают в описаниях Project, Milestone и Task, а также в критериях приёмки. Клик использует тот же open/download маршрут, что файловая панель. |
| 10 | Подключить ссылки к комментариям и трудозатратам | Ссылки работают в комментариях Task и заметках о трудозатратах. Общий компонент сохраняет существующие упоминания людей через `@`; отдельно проверены создание, редактирование и отображение старых записей. |
| 11 | Сделать операции с учётом ссылок | Переименование и «заменить новым файлом» атомарно обновляют все ссылки текущего Project. Удаление показывает места использования и требует явного отвязывания; при отвязке подпись остаётся обычным текстом. Любая ошибка возвращает и файл, и тексты в исходное состояние. |
| 12 | Встроить файлы в Changes и завершить сквозные сценарии | Бинарные изменения получают понятную семантику добавления/замены/переименования/удаления, текстовые файлы сохраняют Git diff. Проверены direct/worktree режимы, технический менеджер, внешние изменения, read-only состояния, адаптивность и основные browser E2E-сценарии. |

### Границы проверки этапов

- Этапы 1 и 8: как минимум `verify:repository`, с дополнительными профилями реальных потребителей.
- Этапы 2–4: `verify:server`; при изменении drafts/security также `verify:workflow`, при изменении
  общих контрактов также `verify:repository`.
- Этапы 5–7, 9 и 10: `verify:web` плюс соответствующий server-профиль при изменении HTTP-контракта.
- Этапы 11 и 12: затронутые repository/server/web/workflow профили и сквозные
  `verify:e2e-ui`/`verify:e2e-workflow` по фактической границе поведения.
- Полный `verify:local` нужен только если реализация затронет корневую конфигурацию или влияние
  конкретного этапа нельзя будет уверенно ограничить.

Перед началом каждого этапа следует повторно посмотреть фактические изменённые импорты и уточнить
профили проверки. Этот список задаёт минимальную ожидаемую границу, а не разрешение пропускать
затронутые тесты.

## 9. Статус реализации

### Этап 1 — завершён

Реализовано:

- полное repository validation допускает опциональный плоский каталог
  `projects/<project-id>/files/` и не передаёт его содержимое domain YAML parser;
- обычные файлы с любым расширением, без расширения, с пробелами и Unicode считаются непрозрачным
  содержимым и не меняют количество доменных документов;
- введена общая проверка одного файлового имени на переносимый Windows-совместимый набор правил и
  регистронезависимый ключ сравнения;
- validation отклоняет недопустимое имя, конфликт имён без учёта регистра, вложенный каталог,
  symbolic link и специальный filesystem entry; обход пути исключён требованием одного валидного
  сегмента имени;
- нормативные Repository Format и Implementation Plan дополнены каноническим layout, правилами
  имени и стабильными кодами ошибок;
- добавлены unit-тесты правил имени и repository-тесты для бинарного содержимого, ложного `.yaml`,
  Unicode, файлов без расширения, вложенности, symlink и конфликта регистра.

Фактически выполненные проверки:

- первоначальный `corepack pnpm --filter @gitpm/validation typecheck` не запустился: в новом
  worktree ещё отсутствовал `node_modules` и команда `tsc` не была доступна;
- `corepack pnpm install --frozen-lockfile` — успешно;
- первый узкий запуск Vitest после установки зависимостей не загрузил `@gitpm/calendar`, потому
  что workspace dependency closure ещё не была собрана;
- `corepack pnpm --filter @gitpm/validation... build` — успешно;
- `corepack pnpm exec vitest run packages/validation/src/project-files.test.ts packages/validation/src/index.test.ts`
  — успешно: 56 тестов пройдено, один платформенный тест пропущен;
- первый запуск `corepack pnpm verify:repository` остановлен внешним лимитом команды через 124
  секунды до результата (`exit 124`), не из-за ошибки проверки;
- повторный `corepack pnpm verify:repository` с увеличенным лимитом — успешно: frozen install,
  сборка affected packages, lint, 117 repository-тестов, schema contracts и diff whitespace
  пройдены; один платформенный тест пропущен.

Ограничения этапа:

- на Windows невозможно создать в одном каталоге два файла, отличающихся только регистром,
  поэтому интеграционный тест такого состояния выполняется только на case-sensitive платформе;
  общий регистронезависимый ключ отдельно покрыт и работает на Windows;
- этап намеренно не предоставляет список, свойства, чтение или скачивание файлов — это граница
  этапа 2; текущий `discoverRepositoryFiles` сохраняет контракт списка только domain YAML.

### Этап 2 — завершён

Реализовано:

- добавлен read-only domain-сервис файлов Project: проверяет существование owning Project,
  возвращает пустой результат для отсутствующего опционального каталога `files/`, считает файлы и
  общий размер и перечисляет только обычные файлы плоского хранилища;
- список содержит имя, канонический repository-relative путь, точный размер, MIME,
  `inline`/`attachment`, доступные даты текущей рабочей копии и явный источник этих дат; никаких
  manifest, sidecar, YAML-сущностей, реестров или базы данных не создаётся;
- безопасное открытие использует проверку имени одного сегмента, существующие filesystem boundary,
  запрет symlink, `O_NOFOLLOW` где он поддерживается и сверку идентичности файла до и после
  открытия; file handle закрывается после завершения, ошибки или прерывания потока;
- добавлены авторизованные read-only HTTP-маршруты списка, открытия и принудительного скачивания;
  ownership draft проверяется до обращения к domain-сервису, а имя и Project остаются частью
  жёсткой области маршрута;
- `inline` разрешён по консервативному allowlist только для PDF, пассивных растровых изображений и
  текстовых форматов; SVG, HTML, Office, архивы и неизвестные форматы отдаются как `attachment`,
  все ответы используют `nosniff`, `no-store` и безопасный UTF-8 `Content-Disposition`;
- публичные DTO, runtime decoder и стабильные коды ошибок задокументированы в
  `docs/Project_Files_HTTP.md`;
- тестами покрыты MIME/disposition, Unicode, пустой каталог, размеры и свойства, отсутствующие
  файл и Project, другой владелец draft, одинаковое имя в другом Project, symlink, невалидный
  Project ID, hostile filename и percent-decoded разделитель пути.

Фактически выполненные проверки:

- первый `corepack pnpm --filter @gitpm/contracts build` прошёл, а следующий
  `corepack pnpm --filter @gitpm/domain build` выявил локальную неоднозначность Node-типа
  `Stats` (`number | bigint`); тип был уточнён без изменения поведения;
- запущенный следом `corepack pnpm --filter @gitpm/server typecheck` не мог разрешить ещё не
  собранные workspace-зависимости, поскольку предыдущая цепочка остановилась на domain build;
- `corepack pnpm --filter @gitpm/server... build` после исправления — успешно;
- `corepack pnpm exec vitest run packages/contracts/src/index.test.ts packages/domain/src/project-files.test.ts apps/server/src/project-files-api.integration.test.ts`
  — успешно: 3 файла, 32 теста пройдено;
- повторные `corepack pnpm --filter @gitpm/server build` и те же 32 узких теста после проверки
  encoded path и закрытия потока — успешно;
- `corepack pnpm verify:server` — успешно: frozen install, affected build, eslint, 12 файлов и 100
  server-тестов, diff whitespace пройдены;
- `corepack pnpm verify:repository` — успешно: frozen install, affected build, eslint, 8 файлов и
  130 repository-тестов пройдены, один платформенный тест пропущен; schema contracts и diff
  whitespace пройдены.

Ограничения этапа:

- даты помечены как свойства filesystem текущей рабочей копии и не обещают сохранение после
  checkout; Git-даты и история версий будут добавляться только если дальнейший UX действительно
  потребует их без отдельного реестра;
- Office-документы и другие потенциально активные либо неизвестные форматы на этом этапе не
  получают встроенный preview и всегда скачиваются;
- этап намеренно не реализует загрузку, замену, переименование, удаление, web UI или файловые
  ссылки — это границы последующих этапов.

### Этап 3 — завершён

Реализовано:

- добавлен отдельный потоковый маршрут одного файла
  `POST /api/drafts/:draftId/projects/:projectId/files/upload`: тело передаётся как
  `application/octet-stream`, исходными байтами без JSON, base64 и multipart-буферизации; точное
  Unicode-имя, ожидаемый размер, fingerprint и режим `create`/`replace` передаются в заголовках;
- загрузка доступна только владельцу открытого draft с ролью Developer или Maintainer и проходит
  через существующий UI writer mode и оптимистичный fingerprint; read-only роль, другой владелец,
  external writer mode и устаревший fingerprint отклоняются до чтения содержимого;
- `create` никогда не перезаписывает существующий файл, а `replace` требует существующий обычный
  файл с точно тем же именем; отдельно отклоняются конфликты имён без учёта регистра и изменения
  target во время операции;
- поток записывается в уникальный target-local обычный файл с `O_EXCL`/`O_NOFOLLOW`, точный
  объявленный размер сверяется с реально прочитанными байтами, а публикация выполняется атомарно
  без overwrite. Identity и realpath каталога проверяются перед финализацией; cleanup удаляет
  staging только при совпадении inode и не удаляет файл, подменённый внешним процессом;
- после публикации запускается полная `validateRepository`. При ошибке новый файл удаляется, а
  прежнее содержимое замены восстанавливается из удерживаемого обычного file handle; штатный успех
  и штатная ошибка не оставляют временных файлов или backup;
- для файла строго больше 50 MiB сервер требует точное имя в отдельном заголовке подтверждения.
  Это не запрет: независимый hard limit по умолчанию равен 2 GiB и настраивается числом байтов через
  `GITPM_PROJECT_FILE_MAX_UPLOAD_BYTES` или `projectFileMaxUploadBytes` в `config.json`;
- добавлены общий DTO и runtime decoder результата, стабильные HTTP-коды ошибок и русская
  документация wire-контракта. Manifest, sidecar, YAML-сущность, реестр и база данных не появились;
- тестами покрыты raw stream и точные бинарные байты, нулевой размер, Unicode, явная замена,
  конфликт существующего и регистронезависимого имени, роли/ownership/writer mode/fingerprint,
  отсутствующее/ошибочное/верное подтверждение больше 50 MiB, настраиваемый hard limit, неверный
  размер, прерванный поток и cleanup, rollback полной validation, hostile paths, symlink и TOCTOU
  target/staging.

Фактически выполненные проверки:

- первоначальный `corepack pnpm --filter @gitpm/server... build` — успешно;
- первый узкий прогон после добавления тестов выявил три несоответствия тестовой инфраструктуры и
  маппинга ошибок; после исправлений `corepack pnpm exec vitest run packages/contracts/src/index.test.ts packages/domain/src/project-files.test.ts apps/server/src/project-files-api.integration.test.ts`
  — успешно: 3 файла, 47 тестов;
- расширенный узкий прогон после добавления runtime-конфигурации выявил, что wrong Content-Type
  превращался в 500; после исправления и пересборки domain итоговый прогон contracts/domain/server/runtime
  — успешно: 4 файла, 61 тест;
- `corepack pnpm exec eslint` для всех затронутых TypeScript-файлов — успешно;
- финальный `corepack pnpm verify:server` — успешно: frozen install, affected build, eslint,
  12 файлов и 110 server-тестов, diff whitespace пройдены;
- финальный `corepack pnpm verify:repository` — успешно: frozen install, affected build, eslint,
  8 файлов и 138 repository-тестов пройдены, один платформенный тест пропущен; schema contracts и
  diff whitespace пройдены;
- `verify:workflow` не требовался: packages drafts/security и их публичные контракты не менялись,
  реализация использует существующую границу `DraftManager.withUiMutation`.

Ограничения этапа:

- маршрут загружает один файл за запрос; очередь нескольких файлов и прогресс относятся к web UI
  этапа 6;
- дата `lastModified` браузера пока не передаётся, поэтому свойства продолжают показывать только
  фактические даты filesystem рабочей копии;
- переименование, удаление, ссылки, web UI и представление файлов в Changes намеренно не входят в
  этап 3 и остаются этапами 4–12;
- если внешний файловый менеджер одновременно подменяет target или внутреннее случайное
  staging-имя, операция безопасно прекращается и не удаляет чужой inode; такое внешне созданное
  состояние требует обычного обновления/исправления рабочей копии.

### Этап 4 — завершён

Реализовано:

- добавлены авторизованные server/domain-операции переименования и удаления одного обычного файла
  только из плоского каталога текущего Project; оба маршрута доступны Developer и Maintainer,
  проходят через существующий `DraftManager.withUiMutation` и поэтому соблюдают владельца draft,
  writer mode `ui` и ожидаемый fingerprint;
- обе операции проверяют Project ID и owning `project.yaml`, точное исходное имя, Windows-совместимое
  новое имя, отсутствие symlink и не-обычных entries, identity каталога и файла, а также
  регистронезависимые конфликты; exact и case-only rename различаются, а case-only rename переносимо
  работает на Windows через то же target-local transient-переименование;
- rename и delete выполняют полную `validateRepository`; исходный файл временно перемещается в
  уникальную обычную запись `.gitpm-project-file-<uuid>.rename|delete`, которая безопасно сохраняет
  inode для отката, а rename публикует новое имя hard link без overwrite; штатный успех, обычная
  ошибка и успешный rollback удаляют transient только при совпадении identity;
- внешние гонки не перезаписывают и не удаляют чужой файл. Если внешний процесс занял исходное имя
  и безопасный rollback невозможен, возвращается `PROJECT_FILE_ROLLBACK_FAILED`, чужой файл
  сохраняется, а исходное содержимое остаётся в repository-relative transient-записи для ручного
  восстановления; абсолютный путь не попадает в ответ;
- публичные request/response DTO и runtime decoders содержат явный future-ready режим
  `reference_mode: "ignore_unchecked"` и ответ `references.status: "not_checked"`: этап не выдаёт
  ложный ноль и честно не ищет/не переписывает ссылки до этапа 11; неизвестный режим возвращает
  `PROJECT_FILE_REFERENCES_UNSUPPORTED`;
- удаление требует точного `confirmation_name`, возвращает `secure_erase: false` и удаляет файл
  только из текущей версии Git; контракт не обещает физическое стирание носителя или истории;
- `docs/Project_Files_HTTP.md` дополнен маршрутами, wire-примерами, стабильными кодами ошибок,
  правилами transient recovery и границами текущей поддержки; manifest, sidecar, отдельный реестр,
  YAML-сущность и база данных не добавлялись.

Фактически выполненные проверки:

- первый `corepack pnpm --filter @gitpm/domain build` прошёл; первый узкий contracts/domain-прогон
  выявил шесть Windows-ошибок первоначального rollback через удерживаемый file handle и hard link:
  открытый handle мешал rename/unlink и приводил к `EPERM`; механизм был упрощён до единственной
  target-local transient-записи без удерживаемого handle;
- после исправления узкий contracts/domain-прогон — успешно: 2 файла, 43 теста;
- `corepack pnpm --filter @gitpm/server... build` и contracts/domain/server integration — успешно:
  3 файла, 64 теста; отдельный schema-тест затем подтвердил обязательный `reference_mode` и отказ
  неизвестного JSON-поля;
- после окончательного покрытия аварийного recovery и отсутствующего опционального каталога
  `corepack pnpm exec eslint` для затронутых contracts/domain/server-файлов, узкий Vitest и
  `git diff --check` — успешно: 3 файла, 68 тестов; первый server-only запуск нового absent-directory
  теста увидел старый собранный domain `dist`, после явной сборки domain тот же тест прошёл;
- финальный `corepack pnpm verify:server` — успешно: frozen install, affected build, eslint,
  12 файлов и 120 server-тестов, diff whitespace пройдены;
- финальный `corepack pnpm verify:repository` — успешно: frozen install, affected build, eslint,
  8 файлов и 147 repository-тестов пройдены, один платформенный тест пропущен; schema contracts и
  diff whitespace пройдены;
- `verify:workflow` не требовался: packages drafts/security и их публичные границы не менялись,
  обе операции используют уже проверенный `DraftManager.withUiMutation`.

Ограничения этапа:

- поиск, подсчёт, атомарное обновление и отвязывание `[[file:...]]` намеренно не реализованы и
  остаются этапом 11; до него клиент обязан явно выбрать `ignore_unchecked`;
- удаление не является secure erase, а ранее закоммиченное содержимое может остаться в истории Git;
- `PROJECT_FILE_ROLLBACK_FAILED` означает аварийное внешнее вмешательство: это единственный случай,
  когда recoverable transient-запись намеренно остаётся в Project; рабочая копия требует ручного
  восстановления и до него не считается прошедшей полную validation;
- web UI, загрузочная очередь, действия панели, файловые ссылки и Changes не входят в этап 4.

### Этап 5 — завершён

Реализовано:

- web API получил типизированный read-only метод списка файлов Project, который использует общий
  `decodeProjectFileList`, кодирует границы draft/Project в URL и не вводит отдельную browser-side
  модель или источник данных;
- рядом с описанием Project добавлена доступная кнопка `Файлы` со счётчиком; начальная загрузка
  списка независима от основной загрузки рабочей области, поэтому ошибка файлов не скрывает Project,
  а успешное обновление сразу синхронизирует счётчик;
- кнопка открывает существующий правый `EditorDrawer` без навигации и сброса выбранной Task;
  закрытие по Escape, крестику или фону возвращает фокус к прежнему элементу, а кнопка остаётся
  доступной для архивного Project и закрытого read-only draft;
- панель показывает отдельные loading, empty и error-состояния, позволяет повторить загрузку и при
  ошибке обновления сохраняет уже полученный список;
- реализованы плиточный и табличный виды. Общая для всех Project настройка сохраняется в cookie
  `gitpm.projectFiles.view` с безопасным откатом к плиткам при отсутствующем или повреждённом
  значении;
- code-native SVG-пиктограммы различают PDF, Word, таблицы, презентации, изображения, текст/код,
  архивы и неизвестные файлы; длинные, Unicode и похожие на HTML имена выводятся только как текст,
  переносятся в плитке, а полное имя доступно через `title` и доступное имя элемента;
- добавлены русские и английские строки, адаптивная сетка и таблица с размером и доступной датой
  изменения текущей рабочей копии.

Фактически выполненные проверки:

- `corepack pnpm exec vitest run apps/web/src/api.test.ts apps/web/src/features/projects/project-files-panel.test.tsx apps/web/src/features/projects/project-plan-workspace.test.tsx`
  — успешно: 3 файла, 77 тестов пройдено;
- `corepack pnpm verify:web` — успешно: frozen install, сборка web dependency closure, ESLint,
  typecheck, 50 файлов и 432 web-теста, diff whitespace пройдены;
- отдельное ревью исходников в UTF-8 подтвердило корректные русские и Unicode-имена без mojibake;
  `git diff --check` также входит в успешно пройденный профиль.

Ограничения этапа:

- панель намеренно только читает и обновляет список: открытие, скачивание, свойства,
  переименование и удаление относятся к этапу 7, загрузка и drag-and-drop — к этапу 6;
- поиск, сортировка, миниатюры изображений и сохранение позиции списка пока не добавлены; базовый
  этап показывает фактический порядок server-списка и семейные пиктограммы;
- файловые ссылки `[[file:...]]` и представление файлов в Changes остаются этапами 8–12.

### Этап 6 — завершён

Реализовано:

- web API получил потоковый browser-клиент загрузки одного файла поверх существующего маршрута
  Этапа 3: `XMLHttpRequest` отправляет исходный `Blob` как `application/octet-stream`, без JSON и
  base64, кодирует Unicode-имя через `encodeURIComponent`, передаёт точные size, fingerprint,
  режим `create`/`replace` и отдельное подтверждение большого файла, декодирует общий
  `ProjectFileUploadResult`, сохраняет structured error и поддерживает реальный upload progress и
  отмену через `AbortSignal`;
- в правой панели работают множественный выбор и drag-and-drop. Файлы идут последовательно, чтобы
  каждый следующий запрос использовал fingerprint предыдущей успешной мутации; у каждого элемента
  независимо видны ожидание, передача с прогрессом, успех, ошибка, конфликт или отмена. Успешные
  строки не исчезают при частичной ошибке, ошибочный элемент можно повторить, ожидающий или текущий
  — отменить;
- файл размером ровно 50 MiB загружается обычно. Для каждого файла строго больше 50 MiB открывается
  отдельное предупреждение о росте истории Git, клонирования и места на диске; кнопка активируется
  только после точного ввода полного имени с учётом регистра. Несколько больших файлов требуют
  отдельных подтверждений, а выбор нового конечного имени требует подтвердить уже это имя заново;
- известный exact-конфликт предлагает явную замену с прежним именем и ссылками либо ввод другого
  имени; регистронезависимый конфликт с иным casing не предлагает небезопасную замену, только другое
  имя. Серверные `PROJECT_FILE_EXISTS`/`PROJECT_FILE_NAME_CONFLICT` возвращают элемент в то же
  осознанное разрешение конфликта, ничего не перезаписывая молча;
- после каждого успеха список, общий размер, счётчик кнопки и mutation fingerprint обновляются
  сразу; замена делает upsert и не увеличивает count. Закрытый, external-writer или изменённый
  извне draft сохраняет доступ к списку, но отключает выбор и drop; роль остаётся авторитетной
  server-проверкой, и её `403` показывается как ошибка конкретного элемента очереди;
- добавлены русские и английские строки, адаптивное оформление drop-zone, очереди, конфликтов и
  отдельного modal-подтверждения. Открытие/скачивание, свойства, переименование, удаление и ссылки
  не добавлялись и остаются границами следующих этапов.

Фактически выполненные проверки:

- первый typecheck после перехода API-теста с fetch на XHR выявил четыре test-only ошибки старой
  сигнатуры; после исправления `corepack pnpm --filter @gitpm/web typecheck` прошёл;
- ранний узкий прогон API/panel/workspace прошёл: 3 файла, 79 тестов; расширенное покрытие сначала
  выявило реальный откат локального fingerprint к устаревшему prop между двумя элементами очереди,
  после исправления последовательной синхронизации прогоны прошли на 84, затем 85 тестах;
- финальный узкий `corepack pnpm exec vitest run apps/web/src/api.test.ts apps/web/src/features/projects/project-files-panel.test.tsx apps/web/src/features/projects/project-plan-workspace.test.tsx`
  после тестов raw-XHR error/abort, нескольких больших файлов и replace-upsert — успешно: 3 файла,
  89 тестов;
- первый короткий `corepack pnpm verify:web` был остановлен внешним лимитом shell через примерно
  5 секунд (`exit 124`), не ошибкой проверки. Следующий полный прогон до поздних test-only
  дополнений прошёл: 50 файлов, 442 web-теста;
- первый финальный `corepack pnpm verify:web` прошёл install, build, lint и typecheck, но дал один
  невоспроизводимый сбой существующего unrelated App-теста закрытия interface settings: 443 из 444
  тестов прошли. Точный изолированный повтор этого теста прошёл (1 passed, 20 skipped), production и
  unrelated тест не менялись;
- повторный финальный `corepack pnpm verify:web` — успешно: frozen install, affected build, ESLint
  без предупреждений, typecheck, 50 файлов и 444 web-теста, diff whitespace пройден; итоговое время
  профиля 1 минута 26 секунд;
- `verify:server` и `verify:repository` не требовались: server-маршрут и shared contracts Этапа 3
  не менялись, изменения ограничены web API-клиентом, UI, локалями и их фикстурами.

Ограничения этапа:

- очередь использует существующий server-контракт «один файл на запрос» и выполняет передачи
  последовательно; это намеренно сохраняет строгую optimistic fingerprint-цепочку;
- browser `lastModified` пока не передаётся, поскольку server-контракт Этапа 3 такого заголовка не
  имеет; свойства показывают фактические даты текущей рабочей копии по правилам Этапа 2;
- поиск/сортировка и обычные действия с файлом остаются Этапом 7, файловые ссылки и Changes —
  Этапами 8–12.

### Этап 7 — завершён

Реализовано:

- web API получил типизированные операции переименования и удаления поверх существующих server-
  маршрутов Этапа 4 и общих runtime decoder. Каждый сегмент draft, Project и имени кодируется
  отдельно; клиент всегда передаёт честный `reference_mode: "ignore_unchecked"`, точный fingerprint,
  новое имя или полное подтверждаемое имя и не утверждает, что ссылки уже проверены;
- добавлены единые безопасные URL helper для содержимого и скачивания. Плитка или имя в таблице
  открывает новую вкладку через `/content` только при server disposition `inline`; для
  `attachment` сразу используется `/download`. Явное действие `Скачать` всегда использует
  download-маршрут, а имя ответа остаётся ответственностью безопасного server
  `Content-Disposition`; динамический `target` не используется, ссылки имеют `noopener`;
- после выбора файла доступны открыть, скачать, свойства, переименовать и удалить. Свойства
  показывают имя, семейство и media type, человекочитаемый и точный размер, только полученный от
  server repository-relative путь, фактические даты текущей рабочей копии с подписью источника и
  доступность browser preview; абсолютный путь host-системы не строится и не выводится;
- переименование поддерживает Unicode и case-only изменение, предупреждает при смене расширения,
  сохраняет серверные конфликты и ошибки в открытом диалоге. Удаление активируется только после
  точного ввода полного имени с учётом регистра и явно объясняет, что файл удаляется лишь из
  текущей версии, secure erase не выполняется, а содержимое может остаться в истории Git;
- успешные rename/delete сразу обновляют список, общий размер, счётчик Project и optimistic
  fingerprint для следующей операции; ошибка оставляет прежний список и выбор доступными для
  исправления или повтора. В read-only состоянии изменение отключено, но открыть, скачать и
  посмотреть свойства можно;
- вложенные action-диалоги имеют начальный фокус, удерживают Tab/Shift+Tab, закрываются по Escape
  и фону без закрытия внешней панели и возвращают фокус на вызвавшую кнопку. Длинные, Unicode и
  похожие на HTML имена остаются текстом и попадают в URL только после `encodeURIComponent`;
- добавлены русские и английские строки и адаптивные стили выбора, панели действий, свойств,
  предупреждений и подтверждений. Shared contracts и server не менялись.

Фактически выполненные проверки:

- первый production typecheck до тестов прошёл; первый расширенный typecheck после тестов нашёл
  один test-only matcher, отсутствующий в типах проекта, после замены на обычную проверку прошёл;
- первый узкий Vitest-прогон выявил три несовпадения ожиданий тестов с новым доступным UI: старое
  имя плитки, синхронную проверку возврата фокуса до React commit и два ожидаемых экземпляра имени
  после rename. После уточнения production focus lifecycle и однозначных queries итоговый
  `corepack pnpm exec vitest run apps/web/src/api.test.ts apps/web/src/features/projects/project-files-panel.test.tsx apps/web/src/features/projects/project-plan-workspace.test.tsx`
  — успешно: 3 файла, 97 тестов;
- первый полный `corepack pnpm verify:web` успешно прошёл: 50 файлов и 452 web-теста. Добавленная
  после него строгая проверка Tab обнаружила реальный a11y-дефект: событие вложенного диалога
  всплывало во внешний `EditorDrawer` и переводило фокус за пределы modal. После остановки
  всплытия Tab и проверки Tab/Shift+Tab, Escape, backdrop и возврата фокуса тот же typecheck и
  узкий прогон повторно прошли: 3 файла, 97 тестов;
- финальный `corepack pnpm verify:web` после этого исправления — успешно: frozen install, сборка
  dependency closure и production bundle, ESLint без предупреждений, typecheck, 50 файлов и 452
  web-теста, diff whitespace;
- `verify:server` и `verify:repository` не требовались: server-маршруты и shared contracts Этапов
  2 и 4 не менялись, изменения ограничены web API-клиентом, UI, локалями и тестовыми fake API.

Ограничения этапа:

- подсчёт, поиск и обновление `[[file:...]]` ещё не выполняются; UI и wire contract явно показывают
  состояние `not_checked`, а полноценная ссылочная семантика относится к Этапам 8 и 11;
- preview не преобразует Office, SVG или неизвестные файлы и не пытается обойти server allowlist:
  такие файлы скачиваются. PDF, пассивные изображения и текст открываются самим браузером только
  при server disposition `inline`;
- свойства не обещают устойчивые даты создания или изменения после checkout: показаны только
  фактические timestamps текущей рабочей копии с их source. Git-даты, авторы и отдельные скрытые
  метаданные не добавлялись;
- поиск, сортировка, миниатюры и сохранение позиции списка остаются отдельными UX-дополнениями;
  общий parser и renderer файловых ссылок, ссылочные мутации и Changes остаются Этапами 8–12.
